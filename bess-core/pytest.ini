[pytest]
testpaths = tests
# Inclui test_*.py (convencional) e casos_*.py (suite de validação)
python_files = test_*.py casos_*.py
pythonpath = .
addopts = -v --tb=short --strict-markers
markers =
    slow: marca testes que levam mais de 1 s