# Dependencias do agente single (Sprint 2 + Sprint 3)
anthropic>=0.40.0
streamlit>=1.36
plotly>=5.20
python-dotenv>=1.0

# Sprint 3 - FastAPI backend (Fase A)
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.5

# Sprint 3 - Persistencia Supabase (Fase C)
# Usamos postgrest (sub-pacote leve do supabase) direto, em vez do umbrella
# 'supabase>=2.7'. Razao: o umbrella puxa storage3 -> pyiceberg, que tem
# extensao C que exige MSVC para buildar no Windows com Python 3.14.
# postgrest cobre tudo que precisamos para CRUD em tabelas Postgres.
postgrest>=0.16

# Sprint 4 Fase A - Geracao de PDF da proposta executiva
# reportlab: motor de PDF (Platypus para layout estruturado).
# markdown-it-py: parser Markdown -> AST (rapido e confiavel, mesmo motor do
# react-markdown no front).
reportlab>=4.0
markdown-it-py>=3.0

# Sprint 4 Fase B - RAG da base regulatoria via BM25
# rank_bm25: BM25 Okapi puro Python (zero deps). Lightweight (~50 KB).
# pypdf: extracao de texto de PDFs oficiais ANEEL/Planalto. Ja vinha
# implicitamente via reportlab mas declaramos explicitamente.
rank_bm25>=0.2.2
pypdf>=4.0

# bess-core eh importado por path relativo (sem instalar como pacote por enquanto).
